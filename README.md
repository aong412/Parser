To run the parser and tests, you will need to install
- python3
- pytest

To run the parser, run the following command in terminal:
$ python3 main.py

To run the tests, run the following command in terminal:
$ pytest
